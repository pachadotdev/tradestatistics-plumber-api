[Unit]
Description=Plumber API
After=postgresql

[Service]
ExecStart=/usr/bin/Rscript -e "api <- plumber::plumb('/apis/tradestatistics-api/queries.R'); api$run(port=8080, host='0.0.0.0')"
Restart=on-abnormal
WorkingDirectory=/apis/tradestatistics-api/

[Install]
WantedBy=multi-user.target
